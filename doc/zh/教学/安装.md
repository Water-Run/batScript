# 安装  

`batScript`的安装很简单.  

1. 从[GitHub Release]()下载最新的`batScript.zip`, 并解压到合适目录  
2. 将目录下的`env/`添加至环境变量  

完成后, 打开终端, 执行以下命令进行验证.  
编译器:  

```cmd
bsc -Version
```

包管理器:  

```cmd
bspm /v
```

## 插件安装  
