# 基础原理  

`batScript`提供的包管理原理非常基础.  
一个包就是一个`.bs`文件. 包可以依赖于其它包, 但是不能依赖其它文件(即仅可以`lib`导入).  
包有两个属性: 名称和版本. 版本号使用`X.Y.Z`标准化命名. 实际的文件名为: `包名_版本号.bs`.  
所有的包都存储在`batScript`安装路径下的`bslib`文件夹中.  
`bspm`从指定索引站获取连接, 下载对应包到`bslib`中, 并进行对应校验.  
